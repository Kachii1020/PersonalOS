# CLAUDE.md — Personal OS

이 파일은 프로젝트 루트에 둔다. Codex를 쓸 때는 같은 내용을 `AGENTS.md`로 복사한다.

---

## Part A. 행동 규칙

출처: multica-ai/andrej-karpathy-skills의 4원칙. 이 프로젝트에 맞게 항목을 추가했다.

### 1. 코딩 전에 생각하라

**가정하지 마라. 혼란을 숨기지 마라. 트레이드오프를 드러내라.**

- 가정은 명시적으로 말한다. 불확실하면 물어본다.
- 해석이 여러 개면 전부 제시한다. 조용히 하나를 고르지 않는다.
- 더 단순한 방법이 있으면 말한다. 필요하면 반대한다.
- 불명확한 게 있으면 멈춘다. 무엇이 헷갈리는지 이름을 붙이고 묻는다.

### 2. 단순함이 먼저다

**문제를 푸는 최소한의 코드. 추측성 코드 금지.**

- 요청되지 않은 기능 금지.
- 한 번만 쓰는 코드에 추상화 금지.
- 요청하지 않은 "유연성"이나 "설정 가능성" 금지.
- 일어날 수 없는 시나리오에 대한 에러 처리 금지.
- 200줄을 썼는데 50줄로 될 것 같으면 다시 쓴다.

### 3. 외과적 변경

**꼭 필요한 것만 건드린다. 자기가 만든 것만 치운다.**

- 인접한 코드, 주석, 포맷을 "개선"하지 않는다.
- 안 망가진 걸 리팩터링하지 않는다.
- 기존 스타일에 맞춘다.
- 관련 없는 죽은 코드를 발견하면 언급만 하고 지우지 않는다.
- 테스트: 변경된 모든 줄이 요청에 직접 추적되어야 한다.

### 4. 목표 기반 실행

**성공 기준을 정의하고, 검증될 때까지 반복한다.**

작업을 검증 가능한 목표로 바꾼다.

```
1. [단계] → verify: [체크]
2. [단계] → verify: [체크]
3. [단계] → verify: [체크]
```

"동작하게 만들어라" 같은 약한 기준은 계속 확인이 필요하다. 강한 기준은 독립적으로 루프를 돌 수 있게 한다.

### 5. 이 프로젝트 고유 규칙 (추가)

- **스코프 게이트**: SPEC.md의 현재 Phase에 없는 것을 구현하지 않는다. Phase 2 기능이 필요해 보여도 만들지 않고 목록에만 적는다.
- **외부 API는 리포지토리 레이어 뒤에**: 컴포넌트가 `tsdav`, `yahoo-finance2`, Notion SDK를 직접 import하는 코드는 반려. 전부 `lib/repos/*.ts`를 통한다.
- **실패는 조용하지 않다**: 모든 외부 호출 실패는 `sync_state` 또는 `job_runs`에 기록하고 UI에 표시한다. `catch {}` 빈 블록 금지.
- **비밀값은 서버 전용**: `NEXT_PUBLIC_` 접두사가 붙지 않은 환경변수를 클라이언트 컴포넌트에서 읽으면 반려.
- **UI 규칙은 검사 항목**: SPEC.md 6.4의 12개 항목은 취향이 아니라 반려 사유다.
- **검증되지 않은 URL 금지**: RSS 피드, API 엔드포인트를 코드에 넣기 전에 실제로 호출해서 성공을 확인한다. 기억에서 URL을 지어내지 않는다.
- **마이그레이션은 되돌릴 수 있게**: 모든 스키마 변경은 `supabase/migrations/`에 up 파일로 남기고, 기존 파일을 수정하지 않는다.

---

## Part B. 프로젝트 컨텍스트

### 무엇을 만드는가
단일 사용자용 개인 대시보드 웹앱. 전체 스펙은 `SPEC.md`.

### 데이터 소유권 (가장 자주 틀리는 부분)
- **Notion**: 사람이 쓰는 것 (리서치 노트, 위키, 과목 노트, 알고리즘 패턴, 지원 파이프라인). 앱은 **읽기 전용**.
- **Supabase**: 기계가 쓰는 것 (캘린더 미러, 태스크, 뉴스, 브리핑, 퀴즈, 시세, GitHub, 로그).
- **한 레코드는 한쪽에만 산다. 양방향 동기화를 만들지 마라.**

### 현재 Phase
`docs/CURRENT_PHASE.md`에 적힌 값을 따른다. 이 파일이 없으면 Phase 1이다.

### 디렉토리 구조

```
app/                  Next.js App Router 페이지
  (dashboard)/        인증된 영역
  api/
    jobs/             크론이 호출하는 엔드포인트
components/
  ui/                 원시 컴포넌트 (버튼, 카드)
  widgets/            대시보드 카드
lib/
  repos/              데이터 접근 레이어 (외부 SDK는 여기서만)
  integrations/       caldav, notion, news, finance, github
  ai/                 Anthropic 클라이언트 + 프롬프트 + 비용 가드
  design/             토큰
supabase/migrations/
scripts/              수동 실행 스크립트
docs/
  CURRENT_PHASE.md
  DECISIONS.md        결정 로그
  DEFERRED.md         구현 안 한 것들
tests/
.github/workflows/    크론
```

---

## Part C. 작업 프로토콜

### 모든 작업 시작 시

1. `SPEC.md`의 해당 절과 `docs/CURRENT_PHASE.md`를 읽는다.
2. 작업을 원칙 4의 형식(단계 → verify)으로 다시 쓴다.
3. 가정이 있으면 먼저 나열한다.
4. 그 다음 코드를 쓴다.

### 모든 작업 종료 시

1. `npm run typecheck && npm run lint && npm run test`
2. 변경된 파일 목록을 보고, 요청에 직접 추적되지 않는 변경이 있으면 되돌린다.
3. 구현하려다 안 한 것을 `docs/DEFERRED.md`에 한 줄로 적는다.
4. 아키텍처 결정을 내렸다면 `docs/DECISIONS.md`에 3줄로 적는다 (결정 / 이유 / 버린 대안).

### 금지 행동

- 게이트 조건을 통과했다고 **주장**하기. 실행해서 출력을 보여줘야 한다.
- 테스트를 통과시키려고 테스트를 고치기.
- SPEC.md를 마음대로 고치기. 스펙 변경이 필요하면 제안하고 승인을 받는다.
- 여러 Phase를 한 번에 진행하기.
