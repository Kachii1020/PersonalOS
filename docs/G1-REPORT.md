# 게이트 G1 판정: 통과

판정일 2026-08-03 · 실행 `npm run test:g1` (조건 1~7) + Lighthouse/브라우저 측정 (조건 8)

| # | 조건 | 결과 | 증거 |
|---|---|---|---|
| 1 | iCloud 캘린더 목록 저장, 앱 전용 캘린더가 `is_writable=true`로 정확히 1개 | 통과 | 캘린더 7행, 쓰기 가능 1개 = `Personal OS` |
| 2 | 2회 연속 실행 시 두 번째는 ctag 비교로 이벤트 0건 | 통과 | 객체 조회 0회, 반영 0건, 7개 캘린더 전부 skipped |
| 3 | 앱에서 만든 이벤트가 iCloud에 PUT되고 재동기화 후 같은 uid로 존재 | 통과 | `a627fd18-…@personal-os` PUT → 재동기화에서 되읽음 |
| 4 | 브리핑 잡 실행 시 오늘 날짜 행이 `ready`, 섹션 5건 이상 | 통과 | 2026-08-03 ready, 섹션 5건, 모델 `claude-sonnet-5` |
| 5 | AI 호출 1회당 `ai_usage`에 정확히 1행 | 통과 | 호출 1회 → +1행, 비용 $0.124932 |
| 6 | 이번 달 누적 비용이 예산 이상이면 다음 호출 차단 | 통과 | HTTP 402 "월 AI 예산 소진: $10.2482 / $10.00", 사용량 행 변화 없음 |
| 7 | iCloud가 죽어도 대시보드가 500 없이 렌더 | 통과 | `GET /` → 200 (194,733바이트), 상단에 "iCloud 동기화 실패" 배너 |
| 8 | Lighthouse 접근성 90 이상, 모바일 375px 가로 스크롤 없음 | 통과 | 대시보드 100점 / 로그인 100점, 실패 감사 0건. 375px에서 `scrollWidth == clientWidth` (`/`, `/settings` 실측, 나머지 3개 페이지 고정폭 초과 0건) |

```
ℹ tests 7   pass 7   fail 0
✔ G1 — Phase 1 게이트 (144.3s)
```

## 조건 4에 대한 메모

원래 조건은 "`briefing_sections`가 6건(고정 5 + 회전 1) 이상"이었다. SPEC 5.2의 고정 섹터가
`finance / ai / semiconductor / it` 4개 + `rotating` 1개 = 5개라 6건이 나올 수 없었고,
섹터×언어로 쪼개면 출력 토큰이 3배가 되어 5.5의 월 $10 예산을 넘긴다.
2026-08-03 승인을 받아 SPEC 7절을 5건으로 정정했다 — 조건을 완화한 것이 아니라
5.2의 섹터 수와 맞춘 것이다.

## 게이트 실행 비용

유료 AI 호출은 1회($0.12)다. 조건 4와 5는 같은 실행을 근거로 판정하고,
조건 6은 예산 가드가 호출 전에 차단하므로 요금이 발생하지 않는다.

## 이 게이트가 덮지 않는 것

- ICS 시간표(SPEC 5.1b), 퀴즈, 위키, 과목·성적 — Phase 2
- 시세, 리서치 노트, GitHub, 지원 파이프라인 — Phase 3
- 프로덕션 빌드 화면 — 호스티드 Supabase에 `db push` 전이라 로그인이 안 된다 (`docs/DEFERRED.md`)
